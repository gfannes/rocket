#ifndef HEADER_rocket_Motor_hpp_ALREADY_INCLUDED
#define HEADER_rocket_Motor_hpp_ALREADY_INCLUDED

namespace rocket { 

    class Motor
    {
        public:
            Motor(double length, double diameter)
            {
            }

            double thrust() const
            {
                return 0.0;
            }

        private:
    };

} 

#endif
